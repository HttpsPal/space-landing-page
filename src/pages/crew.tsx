import type { NextPage } from "next";
import Navigation from "../components/Navigation";

const Crew: NextPage = () => {
	return (
		<div>
			<Navigation page="Crew" />
		</div>
	);
};

export default Crew;
